Project Goal

Fix UI and data consistency issues in JobVerse, improve filtering accuracy, ensure database-driven statistics, and crawl + generate realistic job data to enrich employer accounts and overall system depth.

Task 0 – Scope & System Review

Review:

Frontend UI (Light theme, layout, text rendering)

Backend filtering APIs

Database schema (jobs, companies, skills, locations)

Allowed changes:

✅ UI fixes (CSS / Tailwind / layout handling)

✅ Backend logic fixes

✅ Data crawling & seeding

Goal:

Improve correctness

Improve data depth

Improve UX consistency

Task 1 – Fix Text Color Issue on AI Interview Page (Light Theme)
Current Issue

On AI Interview page (Light Theme):

Question text color is black

Background / question card is also dark

Text blends into the question container

Expected Behavior

Question text must:

Be clearly readable in light theme

Have sufficient contrast with question card background

Required Actions

Frontend:

Fix text color specifically for:

AI Interview questions

Light theme only

Ensure theme-based color variables are applied correctly

Do NOT affect dark theme styling.

Task 2 – Fix Job & Company Filter by Location
Current Issue

On job listing & company pages:

Location filter does not return correct results

Filtered data does not match selected location

Expected Behavior

Location filter should:

Accurately return jobs / companies by:

City

Province

Country (depending on system design)

Required Actions

Backend:

Review filter logic (query, JOIN, LIKE, normalization)

Ensure location values are:

Normalized

Consistent between DB and filter input

Frontend:

Ensure correct filter parameters are sent to API

Task 3 – Fix Long Text Overflow in Job Description & Cards
Current Issue

In job description and other cards:

A single very long word/string

Causes content to overflow outside its container

Breaks layout

Expected Behavior

Long text should:

Wrap correctly

Or be truncated gracefully

Never break layout

Required Actions

Frontend:

Apply proper CSS rules:

word-break

overflow-wrap

truncate / line-clamp where appropriate

Apply fix consistently to:

Job description

Company cards

Skill tags

Other text containers

Task 4 – Sync “Hot Skills” Section with Database (Homepage)
Current Issue

On JobVerse homepage:

“Hot Skills” section uses static or incorrect data

Does not reflect real database statistics

Expected Behavior

“Hot Skills” should:

Be calculated from database

Reflect:

Skills most frequently required in job postings

Real usage frequency

Required Actions

Backend:

Implement query to:

Count skill usage across jobs

Sort by popularity

Frontend:

Display returned data dynamically

Ensure:

Data updates automatically when jobs change

Task 5 – Crawl & Generate Job Data for Employers (Data Enrichment)
Goal

Increase data depth and realism of the system

Automatically generate job postings for employer accounts

Expected Outcome

Employer accounts have:

Multiple realistic job postings

Diverse roles, skills, locations

System looks “alive” and production-like

Required Actions

Data Crawling:

Crawl job data from public job sites (title, skills, description, location)

Clean and normalize crawled data

Data Generation:

Map crawled jobs to existing employer accounts

Randomize:

Salary

Experience level

Skills

Database Seeding:

Insert jobs safely

Avoid duplicates

Maintain referential integrity

⚠️ Ensure crawling respects:

Public data only

No private or restricted content

Task 6 – Final Validation & UX Review
Checklist

AI Interview text readable in light theme

Location filter works correctly

Long text no longer breaks layout

Hot Skills reflect real database data

Employers have rich, realistic job listings

Task 7 – Improve Sign In / Sign Out UI & Authentication UX
Current Issues

On Sign In / Sign Up pages:

There is a “demo account” section displayed at the bottom.

This section is no_attach longer needed and should be removed.

When a user enters an incorrect password:

The UI does not show a clear or user-friendly error message.

When a user logs out:

The system does not redirect the user properly.

Expected Behavior

Sign In / Sign Up UI

Remove the demo account section completely.

Keep the UI:

Clean

Professional

Consistent with the rest of JobVerse design system

Login Error Handling

When login fails due to:

Wrong password

Invalid credentials

The user should see a clear error message, such as:

“Incorrect email or password”

Error message should:

Be visible

Not require page refresh

Not expose sensitive backend details

Logout Behavior

When a user logs out successfully:

Clear authentication state (token, user context)

Redirect user to the homepage (/)

Ensure:

Protected pages are no longer accessible after logout

Required Actions

Frontend:

Remove demo account UI section

Improve error message display on login failure

Handle logout redirect logic

Backend:

Ensure authentication API returns:

Proper HTTP status codes (e.g. 401 for invalid credentials)

Clear error messages consumable by frontend

Validation Checklist

Demo account section is fully removed

Wrong password shows correct error message

Successful logout redirects to homepage

No regression in authentication flow